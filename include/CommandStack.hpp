// keep stack of fixed size with commands
// have registry class instance for motors that keeps track of them and addresses them, accepts setPosition('X', 12) etc
// have tick()
// parses commands that are passed, doesn't read them it'self, use parseCommand, readyForCommand, currentCommand
// commands 
